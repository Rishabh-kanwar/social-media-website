<!-- <link rel="stylesheet" href="<%=assetPath('css/user_profile.css')%>"> -->

<div class="big-container">
    <% if(user.id == profile_user.id){ %>
<div class="req-display">
    <p style="font-size: 25px; ">
       <i style="color: #ffc401da;" class="fas fa-user-check"></i> Friend requests
    </p>
    
    <ol class="friend-req" style="margin-bottom: 40px;">
    <%for(u of list){%>
          <li>
              <p>
              <%if(u.avatar){%>
                 <img class="image-profile-friend" src="<%=u.avatar%>" alt="" height="30" width="30">
               <%}else{%>
                <i style="font-size: 23px;" class="fas fa-user-friends"></i>
                <%}%>
                <a href="/users/profile/<%= u.id%>"><%=u.name%> </a>
                <p>
              <a class="req-design"  href="/friends/accept/<%=u._id%>"><i style="color: lightgreen;" class="fas fa-check"></i>Confirm</a>
              <a class="req-design" style="color: red;" href="/friends/reject/<%=u._id%>"><i class="fas fa-times"></i>Reject</a>
            </p>
            </p>
          </li>
          <p></p>
        <%}%>
        </ol>
</div>
<%}%>


<% if(user.id == profile_user.id){ %>
<div id="profile-data">
<img class="main-img" src="<%= profile_user.avatar %>" alt="<%= profile_user.name %>" width="250">


<% if(user.id == profile_user.id){ %>
    <!-- if user matches then show the form -->
<div class="form-diplay">
    <form action="/users/update/<%= profile_user.id %>" enctype="multipart/form-data" method="POST">
        <input type="text" name="name" placeholder="Your Name" value="<%= profile_user.name %>" required>
        <input type="email" name="email" placeholder="Your Email" value="<%= profile_user.email %>" required>
        <input type="file" name="avatar" placeholder="profile-picture" >
        <input type="submit" style="border: 2px solid black; font-weight: 700;"  value="Update" style="cursor: pointer;">
    </form>
</div>



<div class="diplay-data">
<%}else{%>    
    <!-- else show only the profile info -->
    <div style="font-size: 30px;
    margin-left: 15%;
    margin-right: 15%;
    margin-top: 10px;
    margin-bottom: 10px;
    position: relative;
    left: 500px;" >
    <p><%= profile_user.name %></p>
    <p ><%= profile_user.email %></p>
    </div>
    <a id="send-req-button" class="friend-button" href="/friends/sendreq/<%=profile_user._id %>">
    <span><i class="fas fa-user-friends"></i>FRIEND REQ </span>
    </a>

<%}%>
</div>
</div>
<%}else{%>
    <div id="profile-data-other">
       
            <img style="margin-right: 0;" class="main-img" src="<%= profile_user.avatar %>" alt="<%= profile_user.name %>" width="250">
                
            
        
                <!-- else show only the profile info -->
                <div style="font-size: 30px;
                
                margin-top: 10px;
                margin-bottom: 10px;
                " >
                <p><%= profile_user.name %></p>
                <p ><%= profile_user.email %></p>
                </div>
                <a id="send-req-button" class="friend-button" href="/friends/sendreq/<%=profile_user._id %>">
                <span><i class="fas fa-user-friends"></i>FRIEND REQ </span>
                </a>
            
           
        
    </div>
<%}%>
<% if(user.id == profile_user.id){ %>
<div id="user-friends-profile">
    <span class="heading-friends"><i style="color: #ffc401da; margin-right: 7px;" class="fas fa-user-friends"></i>Friends</span>
    <table>
    <%for(u of all_users) {%>
    
        <tr>
                        <%if(u.avatar){%>
                            <td>
                <img class="image-profile-friend" src="<%=u.avatar %>" alt="" width="30" height="30"> 
            <span><a href="/users/profile/<%= u.id%>"> <%=u.name%> </a>  </span>
                            </td>
             
            <%}else{%>
                <td>
                <a href="/users/profile/<%= u.id%>"> <i style="margin-right: 3px;" class="fas fa-user-friends"></i><%=u.name%> </a>
               </td>
               <%}%>
               <td>
                <% if(user.id == profile_user.id){ %>
                <a href="/friends/destroy/<%=u.id%>"> <i style="font-size: 15px; color: red;" class="fas fa-trash-alt"></i></a>
                <%}%>
                </td>
               
        </tr>
        <%}%>
    </table>
</div>
<%}else{%>
 
    <div id="user-friends-profile-other">
    <span style="font-size: 30px;" ><i style="color: #ffc401da; margin-right: 7px;" class="fas fa-user-friends"></i>Friends</span>
    <table>
    <%for(u of all_users) {%>
    
        <tr>
            
                <%if(u.avatar){%>
                            <td>
                <img class="image-profile-friend" src="<%=u.avatar %>" alt="" width="25" height="25"> 
            <span><a href="/users/profile/<%= u.id%>"> <%=u.name%> </a>  </span>
                            </td>
             
            <%}else{%>
                <td>
                <a href="/users/profile/<%= u.id%>"> <i style="margin-right: 3px;" class="fas fa-user-friends"></i><%=u.name%> </a>
               </td>
               <%}%>
              
               
        </tr>
        <%}%>
    </table>
</div>
    
    <%}%>
</div>
<script type="text/javascript" src="<%= assetPath('js/friendreq.js') %>"></script>
